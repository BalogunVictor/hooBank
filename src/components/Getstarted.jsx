import styles from '../style';
import { arrowUp } from '../assets';

const Getstarted = () =>  (
    <div className={`${styles.flexCenter} w-[140px] h-[140px] roundedn-full bg-blue-gradient`}>
    </div>
  )

export default Getstarted